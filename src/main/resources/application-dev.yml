server:
  shutdown: immediate
  port: 8088

spring:
  devtools:
    add-properties: true

api:
  base:
    url:
      oauth: http://localhost:9090/auth

hmpps.sqs:
  provider: localstack
  queues:
    audit:
      queueName: audit-queue